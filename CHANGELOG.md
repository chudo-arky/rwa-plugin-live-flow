# Changelog

## [0.17.6] — 2026-09-24

Исправления надёжности: когда источник данных падает, схема и списки больше не выдают прошлое за текущее.

### Исправлено
- 🔴 **Опрос IP у панели при сплошных отказах.** Если все запросы Connections API падали (например, API-токену админки не выдали права на connections и каждый запрос получал 403), тик всё равно считался удачным: без паузы, без пометки в легенде, с запросом ко всем нодам парка каждые 15 с. Теперь неудачная попытка идёт в счёт лимита тика, у ноды, которая не отвечает, своя растущая пауза, а если за тик не прошёл ни один запрос — общий backoff и «опрос IP у панели: ошибка» в легенде. Ответ без `jobId` тоже считается отказом.
- 🔴 **Доли веток выходов больше не живут вечно.** Окно метрик очищалось только удачным тиком, поэтому при падении `/api/system/nodes/metrics` или пустом ответе линии к WARP и каскадам рисовались по последним долям до перезапуска backend. Доли отдаются, только если последний снимок ноды не старше 90 с; снимки старше окна (10 мин) после перерыва в опросе отбрасываются.
- **Снимок IP с панели не старше порога.** README обещал «снимок до 90 с», но при постоянных отказах по ноде её последний снимок показывался как текущий IP (`◦`) сколько угодно долго. Снимок старше 5 мин и двух полных кругов обхода больше не используется ни в списках, ни в делении по типу сети. Порог растёт с парком: на 100 нодах штатный круг ~8 мин.
- **Списки «по факту» на смешанном парке.** Если хоть у одной строки были `outbound_tags`, из списка выбрасывались все строки без них — пользователи нод со старым агентом и с IP из снимка панели. Теперь фильтруются только строки с тегами, остальные остаются с пометкой «?», а их ноды названы в шапке списка (`unverified_nodes` в ответе).
- **Кривой ответ конфиг-профилей — не «профилей нет».** Ответ без списка `configProfiles` ложился в кэш как удачный, и всем нодам доставался выдуманный DIRECT. Теперь это сбой запроса: кэш держит последний удачный набор с пометкой «устарела», на холодном старте — «конфигурация выходов недоступна».
- В английском интерфейсе выход DIRECT подписывался «Интернет» (в шапке списка по выходу и на карточках при нескольких internet-выходах).
- Предупреждения об опросе IP и метриках пропадали из легенды, когда конфигурация выходов недоступна.

## [0.17.5] — 2026-09-19

Первый стабильный релиз после 0.17.0: всё из пре-релизов 0.17.2–0.17.4 (записи ниже) плюс правки под вышедший remnawave-admin 4.8.3.

### Изменено
- **Списки «кто ушёл в каскад и в выход — по факту» работают на штатной админке.** Тег инбаунда при маршруте по правилу и выбранные аутбаунды (`outbound_tags`) вошли в remnawave-admin **4.8.3** и агент нод **1.8.3** (Case211/remnawave-admin#277) — патчи агента и коллектора, о которых говорил пре-релиз 0.17.4, больше не нужны. На версиях постарше списки остаются «по нодам», как раньше.
- Оговорка в списках «по нодам» говорит, что делать: выбранных аутбаундов нет ни у кого в списке — их присылает агент нод с 1.8.3 при remnawave-admin от 4.8.3 и включённом разборе access.log.
- Проверено на remnawave-admin 4.8.3, агенте нод 1.8.3 и панели Remnawave 3.4.4 (10 нод, каскад, WARP и цепочки): списки по выходам и каскадам строятся по фактическому аутбаунду без патчей.

## [0.17.4] — 2026-09-13 (пре-релиз)

### Добавлено
- **Линии к веткам выходов с трафиком.** Новый источник — счётчики панели по тегам аутбаундов (`GET /api/system/nodes/metrics`, отдельная задача раз в 30 с). Линии идут не только в интернет, но и к WARP, цепочкам и каскадам — туда, где за последние 10 минут реально прошёл трафик ноды; расклад в процентах — в подсказке ноды. Панель отдаёт строки вида `"12.34 GiB"` с шагом 0.01 единицы, поэтому считаются доли, а не Мбит/с, и тихая нода остаётся «без измерений».
- **Кто ушёл в каскад и в выход — по факту.** Списки по клику на прыжок каскада и на выход сужаются до тех, у кого этот аутбаунд встретился в access.log (`device_info.outbound_tags`). Поле дают агент и коллектор админки с патчами; без них остаётся прежний список по нодам-источникам.
- **IP там, где агент не читает access.log** — из карты онлайн-IP панели (`POST/GET /api/connections/by-node`, обход нод по кругу, снимок до 90 с). Такие записи помечены `◦`; тега инбаунда у них нет. Только `by-node`: `by-user` очищает карту в Xray.
- **Метка CDN в конфиге.** Поле `"liveFlow": {"cdn": true}` у инбаунда конфиг-профиля. Есть хоть одна метка — группа «Через CDN» считается только по помеченным входам; меток нет — «Предположительно CDN» по настройкам инбаунда. Xray 26.7.28 поле принимает, панель сохраняет его без пересоздания инбаунда.
- **Подсказки о сборе данных.** Состояние агента ноды (молчит / жив, но лог не читается) и «адреса клиентов не видны» — у инбаунда на loopback не задан `trustedXForwardedFor`, и Xray пишет всех как `127.0.0.1`.
- **Провенанс в легенде** — видно, какой источник недоступен.
- **README: «Адрес клиента за CDN и обратным прокси»** — проверенная на стенде схема для nginx на origin и описание метки CDN.

### Изменено
- Без меток группа называется «Предположительно CDN», в списках — «CDN?»: признак берётся из конфигурации инбаунда, а не из проверки на сети.
- Оговорка в списках каскадов и выходов точнее: выбранный аутбаунд не виден, потому что агент без патча присылает наверх только тег инбаунда.

### Исправлено
- 🔴 **Протухшие IP больше не выдаются за текущие.** Админка закрывает строки подключений только из батча агента, и строка ушедшего пользователя висела открытой сутками (на боевой — 5.6 суток). Строка, чей адрес снимок панели уже не видит и которая старше снимка, отбрасывается — и в списках, и в делении по типу сети.
- Прежний совет README включить `trustedXForwardedFor` без правки nginx открывал клиенту подмену адреса.

### Решения
- Точного режима метрик нет: сырые счётчики требуют своего URL и пароля панели — настройки сверх установки плагина.
- GeoIP только читается, `ip_metadata` плагин не пишет. Настроек у плагина нет.
- Признак «агент жив, но лог не читается» слабый: сверять строки агента с картиной панели нельзя, привязку к ноде переписывает любой агент.

## [0.17.3] — 2026-09-11 (пре-релиз)

### Изменено
- **Сниппеты раскрывает сама панель.** Для профилей со ссылками `{"snippet": "…"}` плагин запрашивает `GET /api/config-profiles/{uuid}/computed-config` (метод `get_config_profile_computed` клиента админки): панель подставляет сниппеты везде — в аутбаундах, правилах, балансерах, во вложенных случаях — ровно так, как отдаёт конфиг ноде. Один запрос на профиль со ссылками, обычные профили — без лишнего запроса. У старой админки без этого метода остаётся прежний путь через список сниппетов. Семантика сбоев не изменилась: ошибка любого запроса — набор профилей не загружен, кэш держит последний удачный с пометкой «устарела», на холодном старте — «недоступна», DIRECT не выдумывается. Подход подсказал пользователь из чата (заметка «Live-Flow-Snippet-Support»).

## [0.17.2] — 2026-09-10

Релиз пре-релиза 0.17.1 (сборка 2) под своим номером, чтобы разные wheel не ходили под одним. Сводная запись: всё ниже плюс изменения сборки от 09.09 (группа «Через CDN», причины «Сеть неизвестна»). Промежуточные внутренние версии наружу не выходили.

### Добавлено
- **Колонка «Каскад» переделана.** Нода-цель каскада остаётся обычной нодой в своём столбце со своими пользователями и выходами. Колонка «Каскад» состоит из карточек-прыжков «через ноду-цель»: в карточку идёт линия из ноды-источника, из неё — к выходам ноды-цели, так виден весь путь (MOW → через HEL → Интернет). Работает и в «столбце», и в «колонках». В подсказке ноды: «каскад → …» у источника и «принимает каскад от …» у цели; в легенде — свой пункт.
- **Цель каскада ищется по IP, а не только по строке адреса.** Адрес аутбаунда и адрес ноды в панели резолвятся (кэш 10 мин, таймаут 2 с, неудача не роняет схему), поэтому «домен в аутбаунде — IP в панели» тоже узнаётся как каскад. Раньше такие ветки рисовались карточками-выходами «нет измерений».
- **Списки по клику на прыжок каскада и на выход**: «Каскад через <нода>» — кто сейчас на нодах-источниках этого каскада; «Выход <тег>» — кто на нодах, у которых есть этот выход (`/cascade/{uuid}/users`, `/exit/{tag}/users`, право `view_users`). В шапке перечислены ноды и оговорка: какой аутбаунд xray выбрал для конкретного пользователя, панель не знает.
- **Карточки можно перетаскивать мышью**: ноды, группы, прыжки каскада и выходы, в обоих режимах; линии идут за карточкой, холст растёт под сдвинутые карточки. Расстановка запоминается в браузере отдельно для «столбца» и «колонок», кнопка «вернуть расстановку» в панели инструментов.
- **Список «кто на ноде» больше не уносит схему.** Переключатель «список: рядом · поверх схемы» (запоминается в браузере). *Рядом* (по умолчанию) — список встаёт рядом, схема держит прежний масштаб, лишняя ширина уходит в прокрутку, нажатая карточка остаётся на том же месте экрана. *Поверх схемы* — список открывается шторкой справа поверх холста, схема не перестраивается. В обоих режимах холст доскролливается к нажатой карточке, если она вне видимой части.

### Изменено
- Цвет каскада — бирюзовый; янтарный остался за линиями группы «Через CDN».
- Цепочка на чужой сервер (не нашу ноду) подписывается адресом назначения вместо «нет измерений».
- Аутбаунды, вынесенные в сниппеты панели (`{"snippet": "имя"}`), раскрываются — раньше WARP или каскад из сниппета на схеме не появлялись (то же исправление вышло в 0.17.2).

### Исправлено (по аудиту пре-релиза)
- **Сбой сниппетов больше не превращается в DIRECT.** Ошибка запроса сниппетов (панель упала, таймаут, кривой ответ) отличается от успешного пустого ответа: профили со ссылками на сниппеты считаются незагруженными, схема держит последний удачный набор с пометкой «конфигурация выходов устарела», а на холодном старте показывает «конфигурация выходов недоступна». Ссылка на несуществующий сниппет не подменяется DIRECT: выходы такой ноды пусты, в легенде — «сниппеты не раскрыты: имя» (`snippets_unresolved` в `/data`). DIRECT по умолчанию — только у ноды без профиля или с профилем без выходов.
- **Геометрия колонки групп в режиме «столбец».** При 1–3 нодах и четырёх группах (мобильная, Wi-Fi/LAN, через CDN, неизвестно) карточки групп прижимались к верху по одной и наезжали друг на друга, а нижняя вылезала за холст. Колонка групп теперь размещается единым блоком с сохранением интервалов, её нижний край всегда входит в высоту холста; сдвинутые вручную карточки расширяют холст всегда, а не только при непустой расстановке. То же для режима «колонки». Добавлены тесты раскладки (`node --test tests/js/`), CI гоняет их вместе с pytest.
- **Списки для выходов с особыми тегами.** Тег с кавычкой ломал выбор карточки (значение подставлялось в CSS-селектор) — карточки ищутся сравнением атрибута. Тег с `/` давал 404 (`/exit/proxy%2Fus/users` не доходил до маршрута) — тег передаётся query-параметром `GET /exit/users?tag=…`, прежний `GET /exit/{tag}/users` оставлен для тегов без `/`. Право `live_flow:view_users` и `Cache-Control: private, no-store` сохранены.


## [0.17.1] — 2026-09-09 (первая сборка, заменена сборкой от 10.09)

### Изменено
- IP хостинга/CDN (Cloudflare и т. п.: `is_hosting`/`is_vpn`/`is_proxy` или `connection_type` datacenter/hosting/vpn/proxy/cdn) больше не считаются «Wi-Fi / LAN» — это «Сеть неизвестна», потому что за CDN нода не видит клиента.
- Подключения с IP 127.0.0.1/приватным (Xray за локальным прокси без проброса заголовков) — «Сеть неизвестна». Если у такого юзера инбаунд с HTTP-транспортом без Reality (по конфигу профиля), причина в подсказке — «за CDN/прокси». Когда прокси пробрасывает X-Forwarded-For, IP настоящий и тип сети определяется как обычно.
- Карточка «Сеть неизвестна» в подсказке показывает причины: за CDN/прокси / агент ноды не сообщил IP / IP ещё без GeoIP, с числом пользователей по каждой (`vpn_split_total.unknown_why` в `/data`).
- **Группа «Через CDN»**: пользователи, чьё подключение пришло через инбаунд за CDN/прокси (по конфигу профиля: HTTP-транспорт без Reality и при этом security none или sockopt.trustedXForwardedFor), выделены в отдельную карточку слева со своими линиями к нодам (оранжевые), долей в подсказке ноды и сводным списком; в строках — метка «CDN». Маршрут приёма важнее типа сети клиента.
- README: раздел про «Сеть неизвестна» и `trustedXForwardedFor` для инбаундов за CDN.

### Исправлено
- Панель «кто на ноде» на широком окне обрывалась на 60 % высоты экрана и уезжала при прокрутке схемы; теперь прилипает к верху и занимает высоту окна.

## [0.17.0] — 2026-08-23

### Добавлено
- **Масштабирование схемы**: кнопки «+ / − / 100 %» в углу схемы, колесо мыши масштабирует к курсору (0.5×–4×), при увеличении схему можно перетаскивать мышью, двойной клик по пустому месту — сброс. Масштаб сохраняется между автообновлениями.
- **Большие парки (30–60 нод)**: схема не ужимается ниже 70 % — холст получает высоту окна и листается колесом; масштабирование при этом — кнопками, `Ctrl/⌘ + колесо` (и пинч на тачпаде) или простым колесом, если схема помещается целиком.
- **Панель инструментов над схемой**: поиск по имени ноды, фильтры «только активные» и «только с трафиком» (в шапке — «показано X из N»), переключатель вида **столбец / колонки**. Настройки запоминаются в браузере.
- **Режим «колонки»**: страницами по 20 карточек с переключением «‹ 1 из 4 ›» рядом с переключателем вида (в подписи — «НОДЫ · 1–20 / 72»); у колонки шины слева и справа, от групп — по одной линии на класс по верхней полосе, стабы шина→карточка по классам, правая шина к выходам по нижней полосе (только к тем, куда реально идут ноды страницы); каскады — по коридору справа. Ни одна линия не проходит сквозь чужие карточки, ничего не обрезается по высоте, при отсутствии выходов правые шины не рисуются.
- Режим «столбец» (по умолчанию) — прежняя раскладка с масштабом и прокруткой; парк больше 50 нод листается страницами по 50 (тот же пейджер, подпись «НОДЫ · 1–50 / 100»).
- **Производительность на больших парках**: схема перерисовывается в DOM только когда изменились данные (а не каждые 5 с), бегущий пунктир идёт шагами (~12 кадров/с вместо 60, при сотнях живых линий — ~6) и только у линий, попадающих в видимую область (остальные стоят, пока их не долистали; пересчёт на скролл/масштаб/resize), штрихи без круглых торцов, слой композитора создаётся только на время перетаскивания.

### Исправлено
- Дубли тегов выходов (один тег в нескольких профилях) больше не рисуются двумя карточками друг на друге.
- В карточке ноды без счётчика пользователей больше не появляется «· undefined».
- Клик по ноде в пролистанной вниз схеме больше не сбрасывает прокрутку холста наверх.

## [0.16.0] — 2026-08-22

Первый публичный релиз. Изменений относительно предыдущих версий нет — это стартовая точка.

### Что умеет плагин
- **Живая схема трафика** внутри remnawave-admin (пункт «Схема трафика», страница `/plugins/live-flow`): пользователи → ноды → выходы, автообновление каждые 5 с, масштаб под размер окна.
- **Группы пользователей по типу сети** слева — «Мобильная сеть», «Wi-Fi / LAN», «Сеть неизвестна» (по `ip_metadata` текущего IP); от каждой группы свои линии к нодам: толщина — число активных этого класса на ноде, бегущий пунктир — идёт VPN-трафик, свой цвет у каждого класса.
- **Ноды в порядке панели** (viewPosition), на карточке — VPN-скорость из счётчиков xray панели и два числа онлайна: реально активных по панели · счётчик ноды (с пингами клиентов с авто-выбором); в тултипе — профиль, инбаунды, сетевая скорость хоста.
- **Выходы из конфиг-профилей** панели (Интернет / Блокировка / WARP / цепочки появляются сами), каскады нода→нода отдельной колонкой.
- **Клик по ноде** — кто на ней сейчас: пользователь (+ Telegram ID, тег), IP (IPv6 с переносом по /64), AS и страна/город, инбаунд, время активности, VPN-скорость пользователя; один пользователь с несколькими IP — одной ячейкой; прокрутка списка не сбрасывается при обновлении.
- **Клик по группе** — сводный список активных этого класса по всем нодам с колонкой «Нода».
- **Живые данные из панели**: фоновый опрос API каждые 15 с (скорость — дельты `trafficUsedBytes`/`usedTrafficBytes`, онлайн — `onlineAt`/`lastConnectedNodeUuid`, окно активности 180 с от текущего времени); при недоступности панели — автоматический фолбэк на синк админки с пометкой в шапке, ошибки опроса — только кодом.
- **Устойчивость**: опрос не накладывается сам на себя, таймауты и backoff, кеш конфиг-профилей и классификации сети, лимит 20 000 пользователей с пометкой `truncated`, безопасные преобразования данных панели и БД.
- **Права и приватность**: `live_flow:view` — схема и агрегаты; `live_flow:view_users` — списки людей (без него UI не предлагает клики, API отвечает 403); JSON с `Cache-Control: private, no-store`; только чтение, внешней телеметрии нет.
- **Русский и английский интерфейс** — язык следует за переключателем админки.
- **Совместимость**: remnawave-admin Plugin API v1 (страница в админке — с 4.5.4, на более старых — standalone `/api/v2/plugins/live_flow/ui`), панель Remnawave 3.x, Python 3.11+, один backend-worker.
